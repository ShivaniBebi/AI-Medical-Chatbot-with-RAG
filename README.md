AI Medical Chatbot with RAG is an intelligent chatbot that answers questions using information retrieved from uploaded cancer-related medical PDF documents. The application uses Retrieval-Augmented Generation (RAG) with LangChain and FAISS to search the relevant document content, while the Grok API generates responses based only on the retrieved context. This approach helps ensure that answers remain grounded in the provided medical documents rather than relying on general knowledge.
